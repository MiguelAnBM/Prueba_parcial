package poo.principal;

/**
 * CLASE PRINCIPAL — Integración de todos los temas POO
 *
 * INSTRUCCIONES:
 *
 * Agrega los imports necesarios al inicio del archivo y luego
 * completa cada sección dentro del método main():
 *
 * SECCIÓN 1 — Constructores y Objetos:
 *   - Crea un objeto Persona usando el constructor por DEFECTO.
 *   - Crea otro objeto Persona usando el constructor CON PARÁMETROS.
 *   - Imprime ambos objetos.
 *
 * SECCIÓN 2 — Métodos GET y SET (Encapsulación):
 *   - Modifica la primera Persona usando los métodos SET.
 *   - Intenta asignar una edad inválida (ej: 250) y observa el resultado.
 *   - Luego asigna una edad válida.
 *   - Imprime el nombre completo y la edad usando los métodos GET.
 *
 * SECCIÓN 3 — Herencia:
 *   - Crea un objeto Estudiante con todos sus datos.
 *   - Crea un objeto Profesor con todos sus datos.
 *   - Asigna un promedio al estudiante con setPromedio().
 *   - Imprime si el estudiante está aprobado (estaAprobado()).
 *   - Imprime el salario anual del profesor (calcularSalarioAnual()).
 *
 * SECCIÓN 4 — Polimorfismo / Sobreescritura:
 *   - Crea una Lista de tipo Persona.
 *   - Agrega dentro: la Persona del paso 1, el Estudiante y el Profesor.
 *   - Recorre la lista con un for-each y llama a presentarse() en cada elemento.
 *   - Observa cómo el mismo método produce resultados distintos según el objeto.
 *
 * SECCIÓN 5 — Polimorfismo / Sobrecarga:
 *   - Crea un objeto Calculadora.
 *   - Llama a TODAS las versiones sobrecargadas de sumar() y calcularArea().
 *   - Imprime cada resultado.
 *
 * SECCIÓN 6 — Clases Abstractas e Interfaces:
 *   - Intenta crear un objeto FiguraGeometrica directamente y observa el error.
 *   - Crea un Circulo (radio=5), un Rectangulo (base=4, altura=6)
 *     y un Triangulo (lados 3, 4, 5). Usa el color que prefieras.
 *   - Almacénalos en una Lista de tipo FiguraGeometrica.
 *   - Recorre la lista y llama a describir(), dibujar() y mostrarTipo()
 *     en cada figura.
 *
 * SECCIÓN 7 — Resumen de Pilares:
 *   - Imprime un resumen indicando qué clase del proyecto representa
 *     cada pilar: Encapsulación, Herencia, Polimorfismo, Abstracción.
 */
public class Main {

    public static void main(String[] args) {

        // SECCIÓN 1 — Constructores y Objetos


        // SECCIÓN 2 — GET y SET (Encapsulación)


        // SECCIÓN 3 — Herencia


        // SECCIÓN 4 — Sobreescritura (Polimorfismo en tiempo de ejecución)


        // SECCIÓN 5 — Sobrecarga (Polimorfismo en tiempo de compilación)


        // SECCIÓN 6 — Clases Abstractas e Interfaces


        // SECCIÓN 7 — Resumen de Pilares

    }
}
